- name: install and run nginx
  hosts: 172.31.27.94
  #hosts: all
  become: yes
  serial: 3
  tasks:
    - name: install nginx
      ansible.builtin.package:
       name: nginx
       state: present

    - name: Run nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes